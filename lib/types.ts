export type ConfigOptions = {
  configServerUrl: string;
  appId: string;
  env: string;
  token: string;
  clusterName: string;
  namespaceName: string;
};

export type ConfigContentType = {
  content: string;
}
